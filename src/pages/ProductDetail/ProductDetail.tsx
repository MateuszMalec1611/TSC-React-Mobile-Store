import { motion } from 'framer-motion';
import pageTransitionFM from '@pages/pageTransition';

const ProductDetail: React.FC = () => {
    return <motion.div {...pageTransitionFM}>ss</motion.div>;
};

export default ProductDetail;
